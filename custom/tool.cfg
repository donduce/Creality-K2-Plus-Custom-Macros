################################################################################
# Creality K2 macros for better ORCA Sliser compatibility and other improvements
# Based on ideas: https://github.com/jamincollins great thanks!
# GPL-3.0 license 
# Author: Mikhail Ivanov masluf@gmail.com

[respond]

[extruder]
min_extrude_temp: 0

[save_variables]
filename: ./mnt/UDISK/printer_data/config/custom/custom_vars.cfg

[delayed_gcode INIT_VARS_CFG]
initial_duration: 10
gcode:
    {% set svv = printer.save_variables.variables %}
    {% if svv.tool is not defined %}
        SAVE_VARIABLE VARIABLE=tool VALUE=-1
        SAVE_VARIABLE VARIABLE=temp VALUE=240
        SAVE_VARIABLE VARIABLE=velocity VALUE=200 # (max_flowrate / 2.4) * 60
        SAVE_VARIABLE VARIABLE=filament_type VALUE='""'
        SAVE_VARIABLE VARIABLE=addr VALUE=1
        SAVE_VARIABLE VARIABLE=num VALUE='"A"'
    {% endif %}

[gcode_macro CHECK_REFILL]
# pipe length in mm from the CFS to the toolhead - the value must be slightly less than the actual length.* If you see a lot of wastage, increase this value.
variable_cfs_pipe_length: 2000
# Exclude refill on:
variable_refill_roles: ["Perimeter", "ExternalPerimeter", "OverhangPerimeter"]
########################## !!!!! Do not touch next variables !!!!! ######################
# variable for storage current line type.
variable_role: "none"
# variable for storage next tool for autorefill. 
variable_next_compatible_tool: -1
# variable for storage CFS runout length.
variable_runout_length: -1
variable_tnn: '"T1A"'
variable_addr: 1
variable_num: '"A"'
variable_num_byte: 1
########################################################################################
gcode:
    SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=role VALUE='"{params.ROLE|string}"'

[delayed_gcode REFILL_PURGE_AND_LOAD]
initial_duration:0
gcode:
    {% set svv = printer.save_variables.variables %}
    {% set TEMP = svv.temp|default(240)|int %}
    {% set VELOCITY = svv.velocity|default(200.0)|float %}
    
    {% if printer['filament_switch_sensor filament_sensor'].filament_detected == False %}
        UPDATE_DELAYED_GCODE ID=REFILL_PURGE_AND_LOAD DURATION=0
        {action_respond_info ("REFILL_PURGE_AND_LOAD: Finish purge, load")}
        {% set TNN = printer["gcode_macro CHECK_REFILL"].tnn %}
        {% set ADDR = printer["gcode_macro CHECK_REFILL"].addr %}
        {% set NUM = printer["gcode_macro CHECK_REFILL"].num %}
        {% set NUM_BYTE = printer["gcode_macro CHECK_REFILL"].num_byte %}
        {% set next_compatible_tool = printer["gcode_macro CHECK_REFILL"].next_compatible_tool %}
        # Toolhead is empty. Ready to load new material.
        # # Load new
        _CFS_LOAD ADDR={ADDR} NUM_BYTE={NUM_BYTE} TNN={TNN} NUM={NUM}
        
        # # save variables
        SAVE_VARIABLE VARIABLE=tool VALUE={next_compatible_tool}
        SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
        SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'

        SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=runout_length VALUE=-1
        SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=next_compatible_tool VALUE=-1

         # Enable motion buffer
        BOX_ENABLE_CFS_PRINT ENABLE=1
        
        # Purge new
        # _PURGE_AND_CLEAN VELOCITY={VELOCITY} TEMP={TEMP}
        # RESUME
        RESUME
        M400
        RESTORE_GCODE_STATE  NAME=refill_hop MOVE=1 MOVE_SPEED=800
                # G91
                # G1 E-1.0 F2400
                # G1 Z0.4 F20000
                # M400
                # G90
        # restart watchdog
        UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
    {% else %}
        {action_respond_info ("REFILL_PURGE_AND_LOAD")}
        _PURGE_AND_CLEAN VELOCITY={VELOCITY} TEMP={TEMP}
        M400
        UPDATE_DELAYED_GCODE ID=REFILL_PURGE_AND_LOAD DURATION=1
    {% endif %}

[delayed_gcode REFILL_WATCHDOG]
initial_duration:0
gcode:
    {% set cfs_pipe_length = printer["gcode_macro CHECK_REFILL"].cfs_pipe_length|float %}
    {% set runout_length = printer["gcode_macro CHECK_REFILL"].runout_length|float %}
    {% set current_length = printer['print_stats'].filament_used|float %}
    {% set diff = current_length-runout_length %}
    {action_respond_info ("REFILL_WATCHDOG, Runout length: " ~ runout_length ~ " Current length: " ~ current_length ~ " Diff: " ~ diff)}

    {% if diff > cfs_pipe_length * 5 / 6 %}
        # Emergency refill W/O check roles (for Vase mode or other very long perimeters)
        {% set role = printer["gcode_macro CHECK_REFILL"].role %}
        {action_respond_info ("Ready to try EMERGENCY refill on " ~ role)}
        # disable watchdog
        UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=0
        # Check that compatible filament found
        {% set next_compatible_tool = printer["gcode_macro CHECK_REFILL"].next_compatible_tool %}
        {% if next_compatible_tool != -1 %}
            #Compatible tool found. Refill
            { action_respond_info("Compatible tool found: " ~ next_compatible_tool)}
            {% set TNN = 'T' %}
            {% set ADDR = 1 %}
            {% set NUM = 'A' %}
            {% set NUM_BYTE = 1 %}

            {% if next_compatible_tool < 4 %}
                {% set TNN = TNN+'1' %}
            {% elif next_compatible_tool < 8 %}
                {% set TNN = TNN+'2' %}
                {% set ADDR = 2 %}
            {% elif next_compatible_tool < 12 %}
                {% set TNN = TNN+'3' %}
                {% set ADDR = 3 %}
            {% else %}
                {% set TNN = TNN+'4' %}
                {% set ADDR = 4 %}
            {% endif %}

            {% if next_compatible_tool % 4 == 0 %}
                {% set TNN = TNN+'A' %}
            {% elif next_compatible_tool % 4 == 1 %}
                {% set TNN = TNN+'B' %}
                {% set NUM = 'B' %}
                {% set NUM_BYTE = 2 %}
            {% elif next_compatible_tool % 4 == 2 %}
                {% set TNN = TNN+'C' %}
                {% set NUM = 'C' %}
                {% set NUM_BYTE = 4 %}
            {% else %}
                {% set TNN = TNN+'D' %}
                {% set NUM = 'D' %}
                {% set NUM_BYTE = 8 %}
            {% endif %}

            SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=tnn VALUE='"{TNN}"'
            SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=addr VALUE={ADDR}
            SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=num VALUE='"{NUM}"'
            SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=num_byte VALUE={NUM_BYTE}

            { action_respond_info("REfill to: " ~ TNN)}
            # PAUSE
            M400
            SAVE_GCODE_STATE NAME=refill_hop
            G91
            G1 E-1.0 F2400
            G1 Z0.4 F20000
            M400
            G90
            PAUSE_B
            BOX_GO_TO_EXTRUDE_POS
            _GO_CLEAN
            # purge old
            
            SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
            BOX_SET_BOX_MODE ADDR={ADDR} MODE="IDLE"
            UPDATE_DELAYED_GCODE ID=REFILL_PURGE_AND_LOAD DURATION=1
        {% else %}
            # Compatible tool not found. Just pause
            # TODO: Add Manual refill.
            { action_respond_info("No compatible tool found. Just PAUSE")}
            PAUSE
        {% endif %}
    {% elif diff > cfs_pipe_length * 3 / 4 %}
        {% set refill_roles = printer["gcode_macro CHECK_REFILL"].refill_roles %}
        {% set role = printer["gcode_macro CHECK_REFILL"].role %}
        UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=5
        #Exclude refill on some line types for reduce artefacts
        {% if role not in refill_roles %}
            # Refill process
            {action_respond_info ("Ready to try refill on " ~ role)}
            # disable watchdog
            UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=0
            # Check that compatible filament found
            {% set next_compatible_tool = printer["gcode_macro CHECK_REFILL"].next_compatible_tool %}
            {% if next_compatible_tool != -1 %}
                #Compatible tool found. Refill
                { action_respond_info("Compatible tool found: " ~ next_compatible_tool)}
                {% set TNN = 'T' %}
                {% set ADDR = 1 %}
                {% set NUM = 'A' %}
                {% set NUM_BYTE = 1 %}

                {% if next_compatible_tool < 4 %}
                    {% set TNN = TNN+'1' %}
                {% elif next_compatible_tool < 8 %}
                    {% set TNN = TNN+'2' %}
                    {% set ADDR = 2 %}
                {% elif next_compatible_tool < 12 %}
                    {% set TNN = TNN+'3' %}
                    {% set ADDR = 3 %}
                {% else %}
                    {% set TNN = TNN+'4' %}
                    {% set ADDR = 4 %}
                {% endif %}

                {% if next_compatible_tool % 4 == 0 %}
                    {% set TNN = TNN+'A' %}
                {% elif next_compatible_tool % 4 == 1 %}
                    {% set TNN = TNN+'B' %}
                    {% set NUM = 'B' %}
                    {% set NUM_BYTE = 2 %}
                {% elif next_compatible_tool % 4 == 2 %}
                    {% set TNN = TNN+'C' %}
                    {% set NUM = 'C' %}
                    {% set NUM_BYTE = 4 %}
                {% else %}
                    {% set TNN = TNN+'D' %}
                    {% set NUM = 'D' %}
                    {% set NUM_BYTE = 8 %}
                {% endif %}

                SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=tnn VALUE='"{TNN}"'
                SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=addr VALUE={ADDR}
                SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=num VALUE='"{NUM}"'
                SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=num_byte VALUE={NUM_BYTE}

                { action_respond_info("REfill to: " ~ TNN)}
                # PAUSE
                M400
                SAVE_GCODE_STATE NAME=refill_hop
                G91
                G1 E-1.0 F2400
                G1 Z0.4 F20000
                M400
                G90
                PAUSE_B
                BOX_GO_TO_EXTRUDE_POS
                _GO_CLEAN
                # purge old
                
                SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
                BOX_SET_BOX_MODE ADDR={ADDR} MODE="IDLE"
                UPDATE_DELAYED_GCODE ID=REFILL_PURGE_AND_LOAD DURATION=1
                    
            {% else %}
                # Compatible tool not found. Just pause
                # TODO: Add Manual refill.
                { action_respond_info("No compatible tool found. Just PAUSE")}
                PAUSE
            {% endif %}
        {% endif %}
    {% else %}
        UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=5
    {% endif %}
    # {% set ns = namespace(next_compatible_tool=-1) %}

[delayed_gcode CFS_WATCHDOG]
initial_duration:0
gcode:
    {% set svv = printer.save_variables.variables %}
    {% set tool = svv.tool|default(0)|int %}
    {% set s_addr = "T" + svv.addr|string %}

    # {action_respond_info(printer['box'][s_addr]['vender'][tool % 4])}
    {% if printer['box'][s_addr]['vender'][tool % 4] == "none" %}
        {action_respond_info ("CFS Filament runout")}
        # Disable motion buffer
        BOX_ENABLE_CFS_PRINT ENABLE=0
        # disable watchdog
        UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=0
        SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=runout_length VALUE={printer['print_stats'].filament_used|float}
        
        {% set ns = namespace(next_compatible_tool=-1) %}
        {% set filament_type = svv.filament_type %}
        {% set raw_types = svv.cfs_filament_types|default("")|string %}
        {% set types_list = raw_types.split("|") %}
        #Filament Colors
        {% set raw_colors = svv.cfs_colors|default("")|string %}
        {% set colors_list = raw_colors.split('|') %}
        {action_respond_info("CFS Mapping. Types: " + raw_types + " Colors: " + raw_colors)}

        {% for val in types_list %}
            { action_respond_info("Loop: " ~ loop.index0 ~ " VAL: " ~ val)}
            # skip current tool:
            {% if loop.index0 != tool %}
                { action_respond_info("Loop: " ~ loop.index0 ~ " DIFF TOOL: " ~ tool)}
                # Check type and color
                {% if val == filament_type and colors_list[tool] == colors_list[loop.index0] %}
                    SET_GCODE_VARIABLE MACRO=CHECK_REFILL VARIABLE=next_compatible_tool VALUE={loop.index0}
                    {% set ns.next_compatible_tool = loop.index0 %}
                    { action_respond_info("Same filament found: " ~ ns.next_compatible_tool)}
                {% endif %}
            {% endif %}
        {% endfor %}

        UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=5
    {% else %}
        UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
    {% endif %}

[gcode_macro PAUSE]
rename_existing: PAUSE_B
gcode:
    {% if printer.pause_resume.is_paused|lower == 'false' %}
        M400
        PAUSE_B
        G91
        {action_respond_info("USER Emited pause")}
        SET_GCODE_VARIABLE MACRO=PRINTER_PARAM VARIABLE=hotend_temp VALUE={printer.extruder.target}
        M104 S140
        G1 E-1.0 F2400
        G1 Z0.4 F20000
        M400
        G90
        BOX_GO_TO_EXTRUDE_POS
        _GO_CLEAN
        SET_GCODE_VARIABLE MACRO=PRINTER_PARAM VARIABLE=fan2_speed VALUE={printer['output_pin fan2'].value}
        # {% set fspeed = printer['output_pin fan2'].value %}
        # {action_respond_info("fan2_value = %s \n" % (fspeed))}
        SET_GCODE_VARIABLE MACRO=PRINTER_PARAM VARIABLE=fan0_speed VALUE={printer['output_pin fan0'].value}
        # {% set fspeed = printer['output_pin fan0'].value %}
        # {action_respond_info("fan0_value = %s \n" % (fspeed))}
        # SET_PIN PIN=fan2 VALUE=0
        # SET_PIN PIN=fan0 VALUE=0
        M106 P2 S0
        M106 P0 S0
        SET_E_MIN_CURRENT
        SET_PIN PIN=extruder_fan VALUE=0
    {% endif %}

[gcode_macro RESUME]
description: Resume the actual running print
rename_existing: RESUME_B
gcode:
    # {% if printer.pause_resume.resume_err is none or printer.pause_resume.resume_err|lower == 'false' %}
    RESTORE_E_CURRENT
    {% if printer['gcode_macro PRINTER_PARAM'].hotend_temp|int != 0 %}
        {% if printer['gcode_macro PRINTER_PARAM'].hotend_temp|int > printer.extruder.temperature %}
            M109 S{printer['gcode_macro PRINTER_PARAM'].hotend_temp|int}
        {% else %}
            M104 S{printer['gcode_macro PRINTER_PARAM'].hotend_temp|int}
        {% endif %}
    SET_GCODE_VARIABLE MACRO=PRINTER_PARAM VARIABLE=hotend_temp VALUE=0
    {% endif %}
    {% if printer['gcode_macro PRINTER_PARAM'].fan2_speed > 0 %}
        SET_PIN PIN=fan2 VALUE={(printer['gcode_macro PRINTER_PARAM'].fan2_speed * 255 + 0.5)|int}
        # {% set s_value = (printer['gcode_macro PRINTER_PARAM'].fan2_speed * 255 - printer['gcode_macro PRINTER_PARAM'].fan2_min) * 255 / (255 - printer['gcode_macro PRINTER_PARAM'].fan2_min)|float %}
        # M106 P2 S{s_value}    
    {% endif %}
    {% if printer['gcode_macro PRINTER_PARAM'].fan0_speed > 0 %}
        SET_PIN PIN=fan0 VALUE={(printer['gcode_macro PRINTER_PARAM'].fan0_speed * 255 + 0.5)|int}
        # {% set s_value_0 = (printer['gcode_macro PRINTER_PARAM'].fan0_speed * 255 - printer['gcode_macro PRINTER_PARAM'].fan0_min) * 255 / (255 - printer['gcode_macro PRINTER_PARAM'].fan0_min)|float %}
        # M106 P2 S{s_value}    
    {% endif %}
    BOX_GO_TO_EXTRUDE_POS
    {% set svv = printer.save_variables.variables %}
    {% set prev_temp = svv.temp|default(240)|int %}
    {% set prev_velocity = svv.velocity|default(200.0)|float %}
    _PURGE_AND_GO VELOCITY={prev_velocity} TEMP={prev_temp}
    M400
    SET_LIMITS
    SET_PIN PIN=extruder_fan VALUE=1
    RESUME_B VELOCITY=800

[filament_switch_sensor filament_sensor]
pause_on_runout: True
switch_pin: ^!nozzle_mcu:PA11
runout_gcode:
    {action_respond_info("Filament Runout")}
   
[gcode_macro _PURGE_AND_CLEAN]
gcode:
    {% set temp = params.TEMP|default(240)|int %}
    {% set velocity = params.VELOCITY|default(200)|float %}
    # SAVE_GCODE_STATE NAME=purgeandclean
    BOX_GO_TO_EXTRUDE_POS
    # BOX_MATERIAL_FLUSH LEN=50 VELOCITY={velocity} TEMP={temp}
    RESPOND MSG="Purge temp:{temp} velocity:{velocity * 0.7}"
    {% if printer.extruder.target != temp %}
        # RESPOND MSG="Change temp: from {printer.extruder.target} to: {temp}"
        M83
        M109 S{temp}
        G1 E0.5 F60
        M400
        G1 E40 F{velocity * 0.7}
        M400
        G1 E40 F{velocity * 0.7}
    {% else %}
        M83
        # RESPOND MSG="Actual velocity: {velocity * 0.7}"
        G1 E40 F{velocity * 0.7}
        M400
        G1 E40 F{velocity * 0.7}
    {% endif %}
    BOX_BLOW
    M400
    # G4 P200
    _GO_CLEAN
    # RESTORE_GCODE_STATE NAME=purgeandclean

[gcode_macro _PURGE_AND_GO]
gcode:
    {% set temp = params.TEMP|default(240)|int %}
    {% set velocity = params.VELOCITY|default(200)|float %}
    # SAVE_GCODE_STATE NAME=purgeandgo
    BOX_GO_TO_EXTRUDE_POS
    # BOX_MATERIAL_FLUSH LEN=50 VELOCITY={velocity} TEMP={temp}
    RESPOND MSG="Purge temp:{temp} velocity:{velocity * 0.4}, {velocity * 0.2}, {velocity * 0.1}"
    M83
    {% if printer.extruder.target != temp %}
        M83
        M109 S{temp}
        G1 E0.5 F60
        M400
    {% endif %}
    {% if velocity * 0.4 > 150 %}
        G1 E50 F{velocity * 0.4}
    {% else %}
        G1 E50 F150
    {% endif %}

    {% if velocity * 0.2 > 120 %}
        G1 E10 F{velocity * 0.2}
    {% else %}
        G1 E10 F120
    {% endif %}

    {% if velocity * 0.1 > 100 %}
        G1 E10 F{velocity * 0.1}
    {% else %}
        G1 E10 F100
    {% endif %}
    _GO_TO_PLATE
    # RESTORE_GCODE_STATE NAME=purgeandgo

[gcode_macro _GO_CLEAN]
gcode:
    # SAVE_GCODE_STATE NAME=goclean
    G91
    G1 X32 F14000

    G1 Y-4 
    G1 Y6 
    G1 Y-6 
    G1 Y6 
    G1 Y-6 
    G1 Y6 
    G1 Y-2
    # G1 Y6

    G1 X-32 
    G1 X32
    G1 X-32 
    G1 X32
    G1 X-32
    G90
    # RESTORE_GCODE_STATE NAME=goclean
    
[gcode_macro _GO_TO_PLATE]
gcode:
    # SAVE_GCODE_STATE NAME=gotoplate
    G91
    G1 X19 F30000
    G1 Y-28 F30000
    G90
    # RESTORE_GCODE_STATE NAME=gotoplate

[gcode_macro _PARK_ON_PLATE]
gcode:
    M400
    G90
    BOX_GO_TO_EXTRUDE_POS
    _GO_CLEAN
    G91
    G1 X18 F30000
    G90
    BOX_BLOW

# T[initial_no_support_extruder] TEMP=[first_layer_temperature] MAX_FLOWRATE=[filament_max_volumetric_speed]  FILAMENT_TYPE=[filament_type]
[gcode_macro _STAGE5]
variable_sensor_state: False
gcode:
    {% if printer['filament_switch_sensor filament_sensor'].filament_detected == False %}
        # RESPOND MSG="Stage 5"
        BOX_SEND_DATA ADDR={params.ADDR} CMD=16 DATA="{params.NUM_BYTE}50" TIMEOUT=15
        G4 P100
    {% else %}
        # RESPOND MSG="Stage 6"
        BOX_SEND_DATA ADDR={params.ADDR} CMD=16 DATA="{params.NUM_BYTE}60" TIMEOUT=15
    {% endif %}

[gcode_macro _CFS_LOAD]
gcode:
    {% set ADDR = params.ADDR|default(1)|int %}
    {% set NUM_BYTE = params.NUM_BYTE|default("1")|string %}
    {% set NUM = params.NUM|default("A")|string %}
    {% set TNN = params.TNN|default("T1A")|string %}
    # load
    # BOX_EXTRUDE_MATERIAL TNN={TNN}
    ##------------------------------
    BOX_TIGHTEN_UP_ENABLE addr={ADDR} enable="DISABLE"
    #For more info: https://docs.google.com/spreadsheets/d/16-dBGIGJ-zMNRc8hM-vnQLuDPJgmWSqQlyrfId-jeRs/edit?gid=891288709#gid=891288709&range=B1
    BOX_SEND_DATA ADDR={ADDR} CMD=16 DATA="{NUM_BYTE}00" TIMEOUT=15
    BOX_SEND_DATA ADDR={ADDR} CMD=16 DATA="{NUM_BYTE}00" TIMEOUT=15
    G4 P2000
    BOX_DISABLE_HEART_PROCESS TNN={TNN}
    # BOX_EXTRUDE_PROCESS ADDR={ADDR} NUM={NUM} STAGE=4
    BOX_SEND_DATA ADDR={ADDR} CMD=16 DATA="{NUM_BYTE}40" TIMEOUT=15
    G4 P500
    {% for index in range(60) %}
        _STAGE5 ADDR={ADDR} NUM_BYTE={NUM_BYTE}
    {% endfor %}
    BOX_SEND_DATA ADDR={ADDR} CMD=16 DATA="{NUM_BYTE}60" TIMEOUT=15
    G4 P1000
    BOX_SEND_DATA ADDR={ADDR} CMD=16 DATA="{NUM_BYTE}73" TIMEOUT=15
    BOX_ENABLE_HEART_PROCESS TNN={TNN}
    BOX_SET_BOX_MODE ADDR={ADDR} MODE="PRINT" NUM={NUM}
    BOX_TIGHTEN_UP_ENABLE addr={ADDR} enable="ENABLE"
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
    ##------------------------------
[gcode_macro _CFS_UNLOAD]
gcode:
    {% set ADDR = params.ADDR|default(1)|int %}
    {% set NUM = params.NUM|default("A")|string %}
    {% set VELOCITY = params.VELOCITY|default(60)|float %}
     # Cut previous filament
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
    BOX_MOVE_TO_CUT
    BOX_GO_TO_EXTRUDE_POS
    # unload
    G91
    G1 E-10 F{VELOCITY*0.7}
    G1 E-10 F{VELOCITY}
    M400
    G1 E-15 F{VELOCITY}
    # BOX_RETRUDE_MATERIAL
    BOX_SET_BOX_MODE ADDR={ADDR} MODE="IDLE"
    BOX_RETRUDE_PROCESS ADDR={ADDR} NUM=0 TRIGGER="BUFFER"
    G1 E-5 F{VELOCITY}
    G90
    BOX_RETRUDE_PROCESS ADDR={ADDR} NUM=0 TRIGGER="BUFFER"
    BOX_RETRUDE_PROCESS ADDR={ADDR} NUM=0 TRIGGER="MATERIAL"


[gcode_macro TOOLCHANGE]
gcode:
    {% if "xyz" not in printer.toolhead.homed_axes %}
        G28
    {% endif %}
    SAVE_GCODE_STATE NAME=myMoveState
    {% set svv = printer.save_variables.variables %}

    {% set prev_tool = svv.tool|default(0)|int %}
    {% set prev_temp = svv.temp|default(240)|int %}
    {% set prev_addr = svv.addr|default(1)|int%}
    {% set prev_num = svv.num %}
    {% set prev_velocity = svv.velocity|default(200.0)|float %}
    {% set prev_filament_type = svv.filament_type %}

    RESPOND MSG="prev_tool: {prev_tool}, prev_temp: {prev_temp}, prev_velocity: {prev_velocity}, prev_filament_type: {prev_filament_type}"

    {% set tool = params.TOOL|default(0)|int %}
    {% set temp = params.TEMP|default(240)|int %}
    {% set velocity = params.MAX_FLOWRATE|default(2.0)|float %}
    {% set velocity = (velocity / 2.4) * 60 %}
    {% set filament_type = params.FILAMENT_TYPE|default("PLA")|string %}

    RESPOND MSG="tool: {tool}, temp: {temp}, velocity: {velocity}, filament_type: {filament_type}"

    #CFS Autorefill 
    #Filament types
    {% if 'TYPES' in params|upper %}
        #Prevent saving an incorrect values if the macro is not called from a g-code file
        {% set raw_types = params.TYPES|default("")|string %}
        # {% set types_list = raw_types.split("|") %}
        #Filament Colors
        {% set hash = "%c" % 35 %}
        {% set raw_colors = rawparams.split(' ')|last|replace(hash, "") %}
        # {% set colors_list = raw_colors.split('|') %}

        {action_respond_info("CFS Mapping. Types: " + raw_types + " Colors: " + raw_colors)}
        SAVE_VARIABLE VARIABLE=cfs_filament_types VALUE='"{raw_types}"'
        SAVE_VARIABLE VARIABLE=cfs_colors VALUE='"{raw_colors}"'
    {% endif %}
    #--
    
    {% set box1 = printer['box'].T1['state'] %}
    {% set box2 = printer['box'].T2['state'] %}
    {% set box3 = printer['box'].T3['state'] %}
    {% set box4 = printer['box'].T4['state'] %}
    {% set connected_box_count = 0 %}

    {% if box4 == 'connect' %}
        {% set connected_box_count = 4 %}
    {% elif box3 == 'connect' %}
        {% set connected_box_count = 3 %}
    {% elif box2 == 'connect' %}
        {% set connected_box_count = 2 %}
    {% elif box1 == 'connect' %}
        {% set connected_box_count = 1 %}
    {% endif %}

    {% set TNN = 'T' %}
    {% set ADDR = 1 %}
    {% set NUM = 'A' %}
    {% set NUM_BYTE = 1 %}

    #     TNN += ((tool|3)>>2)+1;
    {% if tool < 4 %}
        {% set TNN = TNN+'1' %}
    {% elif tool < 8 %}
        {% set TNN = TNN+'2' %}
        {% set ADDR = 2 %}
    {% elif tool < 12 %}
        {% set TNN = TNN+'3' %}
        {% set ADDR = 3 %}
    {% else %}
        {% set TNN = TNN+'4' %}
        {% set ADDR = 4 %}
    {% endif %}

    {% if tool % 4 == 0 %}
        {% set TNN = TNN+'A' %}
    {% elif tool % 4 == 1 %}
        {% set TNN = TNN+'B' %}
        {% set NUM = 'B' %}
        {% set NUM_BYTE = 2 %}
    {% elif tool % 4 == 2 %}
        {% set TNN = TNN+'C' %}
        {% set NUM = 'C' %}
        {% set NUM_BYTE = 4 %}
    {% else %}
        {% set TNN = TNN+'D' %}
        {% set NUM = 'D' %}
        {% set NUM_BYTE = 8 %}
    {% endif %}

    RESPOND MSG="tool: {tool}, prevTool: {prev_tool}, TNN: {TNN}, boxCount: {connected_box_count}"
    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=0
    {% if tool == 17 %}
        T17
    {% else %}
    # Spoolholder process
        {% if (connected_box_count == 4 and tool == 16) or (connected_box_count == 2 and tool >= 12) or (connected_box_count == 2 and tool >= 8) or (connected_box_count == 1 and tool >= 4) %}
            RESPOND MSG="Spoolholder process"
            {% if printer['filament_switch_sensor filament_sensor'].filament_detected == True and (prev_tool >= tool or prev_tool == -1) %}
                RESPOND MSG="Start print from Spoolholder"
                BOX_ENABLE_CFS_PRINT ENABLE=0
                SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
                # extra purge for different filament
                {% if prev_temp != temp or prev_filament_type != filament_type %}
                    _PURGE_AND_CLEAN VELOCITY={velocity} TEMP={prev_temp}
                    _PURGE_AND_CLEAN VELOCITY={velocity} TEMP={temp}
                {% endif %}
                # purge
                _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == True and prev_tool < connected_box_count * 4 %}
                RESPOND MSG="CFS Tool in toolhead but you try print from spoolholder. Retrude CFS Tool, load spoolholder filament and restart print"
                # Cut previous filament
                SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
                BOX_CUT_MATERIAL
                BOX_GO_TO_EXTRUDE_POS
                # unload
                BOX_RETRUDE_MATERIAL
                RESTORE_GCODE_STATE NAME=myMoveState
                CANCEL_PRINT
            {% else %}
                RESPOND MSG="Toolhead is empty, and you try print from spoolholder. Load spoolholder filament and restart print"
                RESTORE_GCODE_STATE NAME=myMoveState
                CANCEL_PRINT
            {% endif %}
    # CFS Process
        {% else %}
            RESPOND MSG="CFS process"
        # Use same tool again
            {% if printer['filament_switch_sensor filament_sensor'].filament_detected == True and prev_tool == tool %}
                {% if 'TYPES' in params|upper %}
                    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
                    RESPOND MSG="CFS Ready to print {TNN}"
                    BOX_SET_BOX_MODE ADDR={ADDR} MODE='PRINT' NUM={NUM}
                    # purge
                    _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                    SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                    SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                    SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                    SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                    SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                    SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
                    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
                    # Start printing
                {% else %}
                    {action_respond_info("Tool already in Toolhead. CFS Ready to print")}
                {% endif %}
        # Use another tool from CFS, but Spoolholder tool inserted
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == True and (prev_tool >= connected_box_count*4) %}
                {% if 'TYPES' in params|upper %}
                    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
                    RESPOND MSG="Spoolholder Tool in toolhead but you try print from CFS. Retrude spoolholder Tool and restart print"
                    # Cut previous filament
                    BOX_MOVE_TO_CUT
                    BOX_GO_TO_EXTRUDE_POS
                    # unload
                    G91
                    G1 E-25 F{prev_velocity}
                    M400
                    G90
                    RESTORE_GCODE_STATE NAME=myMoveState
                    CANCEL_PRINT
                {% else %}
                    {action_respond_info("Spoolholder Tool in toolhead. Retrude it mannualy and send T command again")}
                    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
                    # Cut previous filament
                    BOX_MOVE_TO_CUT
                    BOX_GO_TO_EXTRUDE_POS
                    # unload
                    G91
                    G1 E-25 F{prev_velocity}
                    M400
                    G90
                {% endif %}
        # Use another tool from CFS
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == True and (prev_tool != tool and prev_tool != -1) %}
                RESPOND MSG="CFS Toolchange, tool: {tool}, prevTool: {prev_tool}"
                    _CFS_UNLOAD ADDR={prev_addr} NUM={prev_num} VELOCITY={prev_velocity}
                    # load
                    {% set max_temp = [temp , prev_temp]|max %}
                    {% set min_velocity = [velocity , prev_velocity]|min %}
                    M104 S{max_temp}
                    _CFS_LOAD ADDR={ADDR} NUM_BYTE={NUM_BYTE} TNN={TNN} NUM={NUM}
                    SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                    SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                    SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                    SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                    SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                    SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
                    # Purge Old
                    {% if prev_temp > temp  %}
                        _PURGE_AND_CLEAN VELOCITY={min_velocity*0.5} TEMP={max_temp}
                    {% endif %}
                    _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={max_temp}
                    _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={temp}
                {% if 'TYPES' in params|upper %}
                    # Purge new
                    _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
                    # Start printing
                {% else %}
                    # Park and switch off hotend
                    M104 S0
                    _PARK_ON_PLATE
                {% endif %}
        # Old tool retruded and new tool need to be loading
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == False and (prev_tool != tool and prev_tool != -1) %}    
                RESPOND MSG="Toolhead is empty. Previous tool from CFS, tool: {tool}, prevTool: {prev_tool}"
                # load
                {% set max_temp = [temp , prev_temp]|max %}
                {% set min_velocity = [velocity , prev_velocity]|min %}
                M104 S{max_temp}
                _CFS_LOAD ADDR={ADDR} NUM_BYTE={NUM_BYTE} TNN={TNN} NUM={NUM}
                # Purge Old
                {% if prev_temp > temp  %}
                    _PURGE_AND_CLEAN VELOCITY={min_velocity*0.5} TEMP={max_temp}
                {% endif %}
                _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={max_temp}
                _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={temp}
                #save tools
                SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
                {% if 'TYPES' in params|upper %}
                    # Purge new
                    _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
                {% else %}
                    # Park and switch off hotend
                    M104 S0
                    _PARK_ON_PLATE
                {% endif %}
        # Old tool retruded and same tool need to be loading
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == False and (prev_tool == tool) %}
                RESPOND MSG="Toolhead is empty and same tool selected  {TNN}"
                # load
                {% if prev_temp != temp  %}
                    {% set max_temp = [temp , prev_temp]|max %}
                    {% set min_velocity = [velocity , prev_velocity]|min %}
                    M104 S{max_temp}
                {% else %}
                    M104 S{temp}
                {% endif %}
                _CFS_LOAD ADDR={ADDR} NUM_BYTE={NUM_BYTE} TNN={TNN} NUM={NUM}
                 # extra purge for different filament
                {% if prev_temp != temp or prev_filament_type != filament_type %}
                    {% if prev_temp > temp  %}
                        _PURGE_AND_CLEAN VELOCITY={min_velocity*0.5} TEMP={max_temp}
                    {% endif %}
                    _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={max_temp}
                    _PURGE_AND_CLEAN VELOCITY={min_velocity} TEMP={temp}
                {% endif %}
                #save tools
                SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
                {% if 'TYPES' in params|upper %}
                    # Purge new
                    _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
                     # Start printing
                {% else %}
                    # Park and switch off hotend
                    M104 S0
                    _PARK_ON_PLATE
                {% endif %}
        # Use new tool or first time use
            {% elif printer['filament_switch_sensor filament_sensor'].filament_detected == False and prev_tool == -1 %}
                RESPOND MSG="Toolhead is empty, start prepare  {TNN}"
                # load
                M104 S{temp}
                _CFS_LOAD ADDR={ADDR} NUM_BYTE={NUM_BYTE} TNN={TNN} NUM={NUM}
                # Purge new
                _PURGE_AND_CLEAN VELOCITY={velocity} TEMP={temp}
                _PURGE_AND_CLEAN VELOCITY={velocity} TEMP={temp}
                #save tools
                SAVE_VARIABLE VARIABLE=tool VALUE={tool}
                SAVE_VARIABLE VARIABLE=temp VALUE={temp}
                SAVE_VARIABLE VARIABLE=addr VALUE={ADDR}
                SAVE_VARIABLE VARIABLE=num VALUE='"{NUM}"'
                SAVE_VARIABLE VARIABLE=velocity VALUE={velocity} 
                SAVE_VARIABLE VARIABLE=filament_type VALUE='"{filament_type}"'
                # Start printing
                {% if 'TYPES' in params|upper %}
                    # Purge new
                    _PURGE_AND_GO VELOCITY={velocity} TEMP={temp}
                    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=10
                     # Start printing
                {% else %}
                    # Park and switch off hotend
                    M104 S0
                    _PARK_ON_PLATE
                {% endif %}
            {% endif %}
        {% endif %}
    {% endif %}
    RESTORE_GCODE_STATE NAME=myMoveState


[gcode_macro T17]
gcode:
    {% set svv = printer.save_variables.variables %}
    {% set tool = svv.tool %}
    {% set prev_velocity = svv.velocity|default(360)|int %}
    {% set prev_addr = svv.addr %}
    {% set prev_num = svv.num %}

    {% set box1 = printer['box'].T1['state'] %}
    {% set box2 = printer['box'].T2['state'] %}
    {% set box3 = printer['box'].T3['state'] %}
    {% set box4 = printer['box'].T4['state'] %}
    {% set connected_box_count = 0 %}

    {% if box4 == 'connect' %}
        {% set connected_box_count = 4 %}
    {% elif box3 == 'connect' %}
        {% set connected_box_count = 3 %}
    {% elif box2 == 'connect' %}
        {% set connected_box_count = 2 %}
    {% elif box1 == 'connect' %}
        {% set connected_box_count = 1 %}
    {% endif %}

    {% if printer['filament_switch_sensor filament_sensor'].filament_detected == True %}
        {% if "xyz" not in printer.toolhead.homed_axes %}
            G28
        {% endif %}
        {% if (connected_box_count == 4 and tool < 16) or (connected_box_count == 2 and tool < 12) or (connected_box_count == 2 and tool < 8) or (connected_box_count == 1 and tool < 4) %}
             RESPOND MSG="CFS Tool in toolhead. Unload: {prev_tool}"
             UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=0
            _CFS_UNLOAD ADDR={prev_addr} NUM={prev_num} VELOCITY={prev_velocity}
        {% else %}
            SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
            BOX_MOVE_TO_CUT
            BOX_GO_TO_EXTRUDE_POS
            # unload
            G91
            G1 E-10 F{prev_velocity*0.7}
            G1 E-10 F{prev_velocity}
            M400
            G1 E-15 F{prev_velocity}
            G90
        {% endif %}
        BOX_NOZZLE_CLEAN
        SAVE_VARIABLE VARIABLE=tool VALUE=-1
    {% else %}
        RESPOND MSG="Toolhead is empty"
    {% endif %}


[gcode_macro END_PRINT_POINT]
gcode:
  {% if "xyz" in printer.toolhead.homed_axes %}
    {% set E = printer["gcode_macro PAUSE"].extrude|float %}
    {% set max_z = printer["gcode_macro PRINTER_PARAM"].max_z_position|float %}
    {% set act_z = printer.toolhead.position.z|float %}
    {% set z_safe = 0.0 %}
    {% if act_z < (max_z / 2) %}
      {% set z_safe = (max_z / 2) - act_z %}
    {% elif act_z < max_z %}
      {% set z_safe = 2.0 %}
    {% endif %}
    G91
    {% if printer.extruder.can_extrude|lower == 'true' %}
      G1 E-1.0 F2400
      G1 E-{E} F2400
    {% else %}
      {action_respond_info("Extruder not hot enough")}
    {% endif %}
    {action_respond_info("END_PRINT_POINT act_z:%s, max_z:%s \n" % (act_z, max_z))}
    {% if act_z < (max_z - 5) %}
        {action_respond_info("END_PRINT_POINT G1 Z2.0 F6000 \n")}
        G1 Z2.0 F20000
    {% endif %}
    _PARK_ON_PLATE
    # {% set y_park = printer.toolhead.axis_maximum.y %}
    # {% set x_park = (printer.toolhead.axis_maximum.x|float - 10.0)/2 %}
    # G1 X{x_park} Y{y_park} F30000
    {% if z_safe > 2.0 %}
      G91
      {% set z_safe = z_safe - 2.0 %}
      G1 Z{z_safe} F800
      G90
    {% endif %}
  {% else %}
    {action_respond_info("end print xyz_axes is NULL\n")}
  {% endif %}

[gcode_macro END_PRINT]
gcode:
    # END_PRINT_BASE
    END_PRINT_POINT
    RESTORE_LIMITS
    M400
    G92 E0
    # BOX_END 
    # BOX_END_PRINT
    Qmode_exit
    EXCLUDE_OBJECT_RESET
    PRINT_PREPARE_CLEAR
    M220 S100
    SET_VELOCITY_LIMIT ACCEL=5000 ACCEL_TO_DECEL=5000
      # TURN_OFF_HEATERS
    {% if printer["output_pin DO_NOT_TURN_OFF_BED"].value|int == 1 %}
        M104 S0 #EXTRUDER
    {% else %}
        M140 S0 #BED
        M104 S0 #EXTRUDER
    {% endif%}
    # M107 P1
    M107 P2
    SET_GCODE_OFFSET Z=0.0 MOVE=1
    WAIT_TEMP_START
    M106 P0 S127
    # M141 S0
    # BOX_NOZZLE_CLEAN
    # BOX_GO_TO_EXTRUDE_POS
    UPDATE_DELAYED_GCODE ID=CFS_WATCHDOG DURATION=0
    UPDATE_DELAYED_GCODE ID=REFILL_WATCHDOG DURATION=0
    SET_PIN PIN=extruder_fan VALUE=0
    
[gcode_macro T0]
rename_existing: T0.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
        TOOLCHANGE TOOL=0 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T1]
rename_existing: T1.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=1 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T2]
rename_existing: T2.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=2 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T3]
rename_existing: T3.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=3 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T4]
rename_existing: T4.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=4 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T5]
rename_existing: T5.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=5 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T6]
rename_existing: T6.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=6 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T7]
rename_existing: T7.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=7 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T8]
rename_existing: T8.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=8 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T9]
rename_existing: T9.1 
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=9 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T10]
rename_existing: T10.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=10 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T11]
rename_existing: T11.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=11 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T12]
rename_existing: T12.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=12 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T13]
rename_existing: T13.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=13 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T14]
rename_existing: T14.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=14 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T15]
rename_existing: T15.1
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=15 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}

[gcode_macro T16]
gcode:
    {% if 'TEMP' in params|upper and 'MAX_FLOWRATE' in params|upper and 'FILAMENT_TYPE' in params|upper %}
        {% set TEMP = params.TEMP|default(220)|float %}
        {% set MAX_FLOWRATE = params.MAX_FLOWRATE|default(6)|float %}
        {% set FILAMENT_TYPE = params.FILAMENT_TYPE|default('PLA')|string %}
    TOOLCHANGE TOOL=16 {rawparams}
    {% else %}
        {action_respond_info("not enough params\n")}
    {% endif%}